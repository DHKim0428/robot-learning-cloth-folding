[project]
name = "robot-learning-cloth-folding"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
      "lerobot[dataset,sarm,training,feetech,viz,smolvla]",
      "pynput>=1.8.1",
]

[tool.uv.sources]
lerobot = { git = "https://github.com/huggingface/lerobot.git", rev = "main" }
